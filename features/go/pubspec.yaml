name: go
description: Go feature module for event planning and task management
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  common:
    path: ../../packages/common
  dart_frog: ^1.1.0
  database:
    path: ../../packages/database
  drift: ^2.22.0
  postgres: ^3.0.2
  uuid: ^4.5.1

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.8
