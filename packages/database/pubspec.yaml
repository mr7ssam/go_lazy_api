name: database
description: Database package with Drift configuration
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  common:
    path: ../common
  dart_frog: ^1.1.0
  drift: ^2.4.0
  drift_postgres: ^1.3.1
  go:
    path: ../../features/go
  postgres: ^3.5.8
  user:
    path: ../../features/user
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.9.0
  drift_dev: ^2.28.3
  test: ^1.25.5
